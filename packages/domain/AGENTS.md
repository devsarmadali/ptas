# Domain Package Instructions

- Keep this package framework-independent and deterministic.
- Every state transition must be explicit, authorized by caller context and tested.
- Do not use current time or random values implicitly; inject them.
- Do not import database, HTTP, React, Vercel or provider SDKs.
- Configuration values must include effective period and approval evidence when legally material.
